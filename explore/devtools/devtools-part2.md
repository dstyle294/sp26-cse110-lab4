1. num1 and num2 are both being passed as strings to calculateSum, therefore, instead of adding the numbers, the code concatenates the two strings. 
2. I would fix it by converting num1 and num2 to Numbers in printSum(), before passing to the function calculateSum. 
